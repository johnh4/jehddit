<h1>New Post</h1>

<%= form_for @post do |f| %>
	<%= f.label :link %>
	<%= f.text_field :link %>

	<%= f.label :content %>
	<%= f.text_area :content %>

	<br>
	<%= f.submit "Post!", class: "btn btn-primary" %>
<% end %>