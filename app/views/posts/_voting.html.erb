<%= form_for(post, url: upvote_post_path(post), method: :patch) do |f| %>
	<%= f.submit "Upvote", class: "btn btn-primary btn-small" %>
<% end %>

<%= form_for(post, url: downvote_post_path(post), method: :patch) do |f| %>
	<%= f.submit "Downvote", class: "btn btn-primary btn-small" %>
<% end %>